# FoodOrderV1 v1.2.0

A quality + UX release: instant sidebar controls, the latest toolchain, and full engineering-quality
parity with the reference repos — shipped as small, verified, pushed commits.

## Highlights

- **Instant sidebar controls.** Language (English ⇄ العربية with RTL), theme (system → light → dark),
  and a **collapsible sidebar** (icon-only rail, persisted) — all change immediately, work before
  sign-in, and persist to device + profile. On mobile the same controls live in the top bar.
- **Latest toolchain.** Vite **8**, Vitest **4**, TypeScript target **ES2024**, `@types/node` 26, and
  every dependency upgraded to its latest compatible version (Firebase 12.16, react-router 7.18,
  lucide 1.x, @capacitor 8.4) — clean install, no `--legacy-peer-deps`.
- **Quality parity with NextRanger / IronNest / TwinzyAI.** 12-plugin ESLint stack (import-sort,
  unused-imports, jsx-a11y, sonarjs, unicorn, security, promise, regexp, testing-library, playwright,
  vitest, prettier-config) applied to every file; Husky pre-commit / commit-msg / pre-push hooks with
  Conventional Commits; knip (dead code) and madge (0 circular) gates; expanded CI into parallel
  quality / e2e / security jobs on Node 26.

## Quality gates (all green)

lint 0 problems · typecheck TS 7.0.2 + TS 5.9.3 · 32 unit/integration tests · 16 Playwright e2e
(responsive shell + instant controls + smoke, desktop & mobile) · production build.

## Android APK

- File: `FoodOrderV1-v1.2.0-debug.apk`
- versionCode 4 / versionName 1.2.0 · min Android 10 (API 29) · target Android 16 (API 36)
- SHA-256: recorded as the `.sha256` asset · built from the `v1.2.0` tagged commit (debug-signed).

## Known limitations & honest scope

- Firestore Security Rules must be deployed to the project before public cloud use
  (`firebase deploy --only firestore:rules`).
- iOS floor is 14.0 (Capacitor 8); build/sign needs macOS + Xcode.
- **Not yet done** (documented in `docs/operations/reference-parity-roadmap.md`): 99%-per-file
  coverage, the module-first architecture migration, Firestore-emulator rules tests, and the 28-phase
  v4 implementation pack. These are the prioritized backlog, not claimed complete.
- Legacy `ihabkhaled/FoodOrder` still exposes an older API key — restrict/rotate it (SEC-LEG-001).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
