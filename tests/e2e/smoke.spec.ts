import { expect, test } from '@playwright/test';
test('register, create bucket and order',async({page})=>{await page.goto('/auth/register');await page.getByLabel('Full name').fill('Demo User');await page.getByLabel('Email').fill(`demo-${Date.now()}@example.com`);await page.getByLabel('Password').fill('Password1');await page.getByRole('button',{name:'Create account'}).click();await page.getByRole('link',{name:'Create bucket'}).click();await page.getByLabel('Bucket title').fill('Breakfast');await page.getByLabel('Item name').fill('Foul');await page.getByLabel('Unit price').fill('20');await page.getByRole('button',{name:'Save'}).click();await page.getByRole('link',{name:'Order now'}).click();await page.getByRole('button',{name:'Increase Foul'}).click();await page.getByRole('button',{name:'Place order'}).click();await expect(page.getByText('Order details')).toBeVisible();});
