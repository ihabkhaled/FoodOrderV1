/** Absolute navigation targets owned by the dashboard module. */
export const DASHBOARD_PATH = '/app';

/** Navigation state marking the dashboard as the origin of a link. */
export const DASHBOARD_NAVIGATION_STATE = { from: DASHBOARD_PATH } as const;
